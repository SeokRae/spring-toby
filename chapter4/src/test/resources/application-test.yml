server:
  port: 8000

spring:
  profiles:
    active: test

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: schema.sql

logging:
  level:
    org:
      hibernate:
        sql: debug
        type: trace
