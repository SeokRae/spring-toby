server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3309/user-db
    username: root
    password: 1234

logging:
  level:
    org:
      hibernate:
        sql: debug
        type: trace
